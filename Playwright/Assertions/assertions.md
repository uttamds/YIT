

## ✅ UI Assertions (Playwright Locator / Page)

* `toBeVisible()` — **UI**

* `toBeHidden()` — **UI**

* `toBeEnabled()` — **UI**

* `toBeDisabled()` — **UI**

* `toBeEditable()` — **UI**

* `toBeChecked()` — **UI**

* `not.toBeChecked()` — **UI**

* `toHaveText()` — **UI**

* `toContainText()` — **UI**

* `not.toHaveText()` — **UI**

* `toHaveCount()` — **UI**

* `toHaveValue()` — **UI**

* `toHaveAttribute()` — **UI**

* `toHaveClass()` — **UI**

* `toHaveId()` — **UI**

* `toHaveURL()` — **UI**

* `toHaveTitle()` — **UI**

* `toHaveCSS()` — **UI**

* `toBeFocused()` — **UI**

---

## 🌐 API Assertions (Playwright APIResponse)

* `toBeOK()` — **API**
* `toHaveStatus()` — **API**
* `toBeTruthy()` — **API / JS**
* `toBeFalsy()` — **API / JS**
* `not.toBeNull()` — **API / JS**

---

## 🟨 JS-Only Assertions (Jest-style / expect)

* `toBe()` — **JS**
* `toEqual()` — **JS**
* `toStrictEqual()` — **JS**
* `toContain()` — **JS**
* `toBeNull()` — **JS**
* `toBeDefined()` — **JS**
* `toBeUndefined()` — **JS**
* `toBeGreaterThan()` — **JS**
* `toBeLessThan()` — **JS**

---

### 🧠 One-line takeaway for students

> **UI assertions → locators**
> **API assertions → responses**
> **JS assertions → variables / data**
